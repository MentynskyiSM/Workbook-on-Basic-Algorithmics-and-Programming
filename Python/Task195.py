a = float(input("Введіть значення A\n"))
u = float(1)
s = u
n = int(input("Введіть значення n\n"))
for i in range(n): # функція range(n) генерує набір 
    u *= -a        # послідовних натуральних чисел з 
    s += u         # з відрізка [0; n - 1] 
print("Значення суми s = %10.3f" % s) 
